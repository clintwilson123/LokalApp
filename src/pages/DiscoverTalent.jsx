import { pageWrapper, card, title } from "../uiStyles";

export default function DiscoverTalent() {
  return (
    <div style={pageWrapper}>
      <div style={card}>
        <h2 style={title}>Discover Local Talent</h2>
        <p>Employers can browse profiles of local applicants.</p>
      </div>
    </div>
  );
}
