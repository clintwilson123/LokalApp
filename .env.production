# Loaded by `vite build` (production mode). .env is gitignored, so this file
# guarantees the deployed bundle always uses the production URL in reset links.
VITE_SITE_URL=https://lokal-app-iota.vercel.app
